apiVersion: v1
kind: Secret
metadata:
  name: {{ media_vpn_secret_name }}
  namespace: {{ media_namespace }}
type: Opaque
stringData:
  WIREGUARD_PRIVATE_KEY: "{{ mullvad_wireguard_private_key }}"
  WIREGUARD_ADDRESSES: "{{ mullvad_wireguard_address }}"
