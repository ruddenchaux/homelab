---
# WireGuard interface name on MikroTik
mikrotik_wg_interface: "wg-vps"

# MikroTik's WireGuard tunnel IP
mikrotik_wg_address: "10.100.0.2/30"

# VPS public IP and WireGuard port
vps_public_ip: "89.167.62.126"
vps_wg_port: 51820

# VPS WireGuard IP (for routing)
vps_wg_ip: "10.100.0.1"

# k8s VLAN — MikroTik already routes this internally, no extra route needed
# (MikroTik IS the gateway for 10.30.0.0/24)
