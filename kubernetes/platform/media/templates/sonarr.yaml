---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sonarr
  labels:
    {{- include "media.labels" . | nindent 4 }}
    app.kubernetes.io/name: sonarr
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "media.selectorLabels" "sonarr" | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "media.selectorLabels" "sonarr" | nindent 8 }}
    spec:
      containers:
        - name: sonarr
          image: {{ .Values.sonarr.image }}
          env:
            {{- include "media.linuxserverEnv" . | nindent 12 }}
          ports:
            - name: http
              containerPort: {{ .Values.sonarr.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /ping
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /ping
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
          volumeMounts:
            {{- include "media.mediaVolumeMount" . | nindent 12 }}
            - name: config
              mountPath: /config
          resources:
            requests:
              memory: 256Mi
              cpu: 100m
            limits:
              memory: 1Gi
      volumes:
        {{- include "media.mediaVolume" . | nindent 8 }}
        - name: config
          persistentVolumeClaim:
            claimName: sonarr-config
---
apiVersion: v1
kind: Service
metadata:
  name: sonarr
  labels:
    {{- include "media.labels" . | nindent 4 }}
    app.kubernetes.io/name: sonarr
spec:
  selector:
    {{- include "media.selectorLabels" "sonarr" | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.sonarr.port }}
      targetPort: http
      protocol: TCP
