---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nzbget
  labels:
    {{- include "media.labels" . | nindent 4 }}
    app.kubernetes.io/name: nzbget
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "media.selectorLabels" "nzbget" | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "media.selectorLabels" "nzbget" | nindent 8 }}
    spec:
      containers:
        - name: nzbget
          image: {{ .Values.nzbget.image }}
          env:
            {{- include "media.linuxserverEnv" . | nindent 12 }}
          ports:
            - name: http
              containerPort: {{ .Values.nzbget.port }}
              protocol: TCP
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
          volumeMounts:
            {{- include "media.mediaVolumeMount" . | nindent 12 }}
            - name: config
              mountPath: /config
          resources:
            requests:
              memory: 256Mi
              cpu: 100m
            limits:
              memory: 512Mi
      volumes:
        {{- include "media.mediaVolume" . | nindent 8 }}
        - name: config
          persistentVolumeClaim:
            claimName: nzbget-config
---
apiVersion: v1
kind: Service
metadata:
  name: nzbget
  labels:
    {{- include "media.labels" . | nindent 4 }}
    app.kubernetes.io/name: nzbget
spec:
  selector:
    {{- include "media.selectorLabels" "nzbget" | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.nzbget.port }}
      targetPort: http
      protocol: TCP
