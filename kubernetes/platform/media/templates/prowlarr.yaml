---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: prowlarr
  labels:
    {{- include "media.labels" . | nindent 4 }}
    app.kubernetes.io/name: prowlarr
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "media.selectorLabels" "prowlarr" | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "media.selectorLabels" "prowlarr" | nindent 8 }}
    spec:
      containers:
        - name: prowlarr
          image: {{ .Values.prowlarr.image }}
          env:
            {{- include "media.linuxserverEnv" . | nindent 12 }}
          ports:
            - name: http
              containerPort: {{ .Values.prowlarr.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /ping
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /ping
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
          volumeMounts:
            - name: config
              mountPath: /config
          resources:
            requests:
              memory: 256Mi
              cpu: 100m
            limits:
              memory: 512Mi
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: prowlarr-config
---
apiVersion: v1
kind: Service
metadata:
  name: prowlarr
  labels:
    {{- include "media.labels" . | nindent 4 }}
    app.kubernetes.io/name: prowlarr
spec:
  selector:
    {{- include "media.selectorLabels" "prowlarr" | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.prowlarr.port }}
      targetPort: http
      protocol: TCP
