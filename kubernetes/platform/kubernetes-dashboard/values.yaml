headlamp:
  clusterRoleBinding:
    create: true
  config:
    oidc:
      callbackURL: "https://dashboard.ruddenchaux.xyz/oidc-callback"
      secret:
        create: false
      externalSecret:
        enabled: true
        name: headlamp-oidc
  ingress:
    enabled: true
    ingressClassName: traefik
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
    hosts:
      - host: dashboard.ruddenchaux.xyz
        paths:
          - path: /
            type: Prefix
    tls:
      - secretName: headlamp-tls
        hosts:
          - dashboard.ruddenchaux.xyz
